module.exports = {
  port: Number(process.env.PORT || 3000),
  arenaRadius: 180,
  arenaMinRadius: 124,
  arenaShrinkStartMs: 12000,
  arenaShrinkDurationMs: 16000,
  playerRadius: 18,
  playerSpeed: 240,
  pushBoostSpeed: 340,
  pushDurationMs: 180,
  pushCooldownMs: 900,
  braceDurationMs: 420,
  braceCooldownMs: 1700,
  braceKnockbackMultiplier: 0.24,
  pushCollisionImpulse: 180,
  baseCollisionImpulse: 95,
  knockbackDragPerSecond: 4.4,
  tickRate: 30,
  roundDurationMs: 35000,
  lobbyCountdownMs: 3000,
  rematchWaitMs: 10000,
  broadcastRate: 30,
  maxPlayersPerRoom: 2
};
